[project]
name = "temporal-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "temporalio>=1.8.0,<1.9",
    "gitpython>=3.1.43",
    "jira>=3.8.0",
    "agent-client-protocol>=0.6.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.23",
]
