name: Docs Deploy

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    concurrency: docs-${{ github.ref }}
    env:
      UV_LINK_MODE: copy
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup uv
        uses: astral-sh/setup-uv@v3
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-path: uv.lock

      - name: Install dependencies
        run: uv sync --extra dev --frozen

      - name: Configure git for mike
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Build docs
        run: uv run --extra dev mkdocs build --strict

      - name: Deploy docs with mike
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          uv run --extra dev mike deploy --push --update-aliases v0.1 latest
          uv run --extra dev mike set-default --push latest

      - name: Ensure CNAME on gh-pages
        run: |
          CNAME_VALUE=$(cat docs/CNAME)
          git fetch origin gh-pages
          git worktree add /tmp/gh-pages gh-pages
          echo "$CNAME_VALUE" > /tmp/gh-pages/CNAME
          cd /tmp/gh-pages
          if ! git diff --quiet -- CNAME; then
            git add CNAME
            git commit -m "docs: ensure CNAME"
            git push origin gh-pages
          fi
